{% extends 'layout.twig' %}

{% block content %}
    <h1>Editando página</h1>
    <form action="{{ path('save') }}" method="post"><br/>
        <input type="hidden" name="id" value="{{ page.id }}"/><br/>
        <input type="text" name="title" value="{{ page.title }}"/><br/>
        <textarea name="content" id="" cols="30" rows="10">{{ page.content }}</textarea><br/>
        <input type="submit" value="Salvar"/>
    </form>
{% endblock %}